export interface NavLink {
  label: string;
  href: string;
}

export interface Avatar {
  id: string;
  imageUrl: string;
  alt: string;
}

export interface Company {
  name: string;
  logo: string;
}